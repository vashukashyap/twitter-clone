import { useState } from "react";

const SigninButton = () => {
  return(
    <>
      <button className="w-full px-16 py-2 border flex items-center justify-center gap-2  rounded-full text-blue-500 text-md font-bold  border dark:border-gray-700">
        Sign in
      </button>
    </>
  );
};

export default SigninButton;
